import type { Aparencia, ConfigSite } from "@/lib/signals/types";
export function SiteFooter({ aparencia, config, footerText }: { aparencia: Aparencia | null; config: ConfigSite | null; footerText?: string }) {
  return <footer className="border-t border-white/10 bg-black/30"><div className="mx-auto grid max-w-7xl gap-8 px-4 py-10 sm:grid-cols-2 sm:px-6 lg:grid-cols-3"><div><div className="flex items-center gap-3"><img src={aparencia?.logo_url || "/logo.webp"} alt="" className="h-10 w-10 object-contain" /><strong className="text-lg">{aparencia?.nome_site || "Slot da Sorte"}</strong></div><p className="mt-3 max-w-sm text-xs leading-5 text-[var(--tenant-muted)]">{footerText || "Sinais atualizados para ajudar você a encontrar os jogos mais interessantes do momento."}</p></div><div><strong className="text-sm">Acesso rápido</strong><div className="mt-3 flex flex-col gap-2 text-sm text-[var(--tenant-muted)]"><a href="#jogos-em-alta">Jogos em alta</a><a href="#todos-os-jogos">Todos os jogos</a>{config?.whatsapp && <a href={config.whatsapp} target="_blank" rel="noopener noreferrer">Grupo no WhatsApp</a>}</div></div><div className="text-xs leading-5 text-[var(--tenant-muted)] lg:text-right"><p>18+ Jogue com responsabilidade.</p><p>Não garantimos lucros e não operamos plataformas.</p><p className="mt-3">© {new Date().getFullYear()} {aparencia?.nome_site || "Slot da Sorte"}.</p></div></div></footer>;
}
